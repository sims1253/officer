% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_table_xml.R
\name{table_subheader}
\alias{table_subheader}
\title{Full-width sub-header row}
\usage{
table_subheader(label, style = NULL)
}
\arguments{
\item{label}{single character string displayed in the divider row.}

\item{style}{paragraph style name applied to the divider cell. If \code{NULL}, the
table's \code{header_style} is used (or \code{"Normal"} if that is also \code{NULL}).}
}
\value{
An object of class \code{table_subheader}.
}
\description{
Mark a sub-header divider row inside a table body: a single row
spanning all columns, containing only a title, that groups the rows that
follow it (e.g. an "endpoint category" banner). Use it as an element of the
\code{value} list passed to \code{\link[=table_xml]{table_xml()}} / \code{\link[=body_add_table_xml]{body_add_table_xml()}}.

A sub-header row also \emph{resets} any active vertical merge: consecutive-value
merging restarts after the divider.
}
\examples{
table_subheader("Endpoint category: Mortality")
table_subheader("Endpoint category: Morbidity", style = "strong")
}
\seealso{
Other docx native-style table helpers:
\code{\link[=body_add_table_xml]{body_add_table_xml()}},
\code{\link[=table_header]{table_header()}},
\code{\link[=table_xml]{table_xml()}}
}
\concept{docx native-style table helpers}
