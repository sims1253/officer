% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_table_xml.R
\name{body_add_table_xml}
\alias{body_add_table_xml}
\title{Add a native-style Word table (with merges)}
\usage{
body_add_table_xml(x, value, ..., pos = "after")
}
\arguments{
\item{x}{an \code{rdocx} object.}

\item{value}{a data.frame, or a list of data.frames and \code{\link[=table_subheader]{table_subheader()}}
objects (see \code{\link[=table_xml]{table_xml()}}).}

\item{...}{arguments passed on to \code{\link[=table_xml]{table_xml()}} (\code{style}, \code{header},
\code{stylenames}, \code{header_style}, \code{col_widths}, \code{align}, \code{merge_consecutive},
\code{tcf}, \code{repeat_header}).}

\item{pos}{where to add the table relative to the cursor: one of \code{"after"}
(default), \code{"before"} or \code{"on"}.}
}
\value{
The \code{rdocx} object \code{x}, invisibly.
}
\description{
Convenience wrapper: build the table markup with \code{\link[=table_xml]{table_xml()}}
and inject it into the document with \code{\link[=body_add_xml]{body_add_xml()}}.
}
\examples{
library(officer)

df1 <- data.frame(endpoint = c("A", "A"), n = c(10, 20))
df2 <- data.frame(endpoint = c("B", "B"), n = c(5, 7))

doc <- read_docx()
doc <- body_add_table_xml(
  doc,
  value = list(
    table_subheader("Category 1"),
    df1,
    table_subheader("Category 2"),
    df2
  ),
  style = "table_template",
  merge_consecutive = "endpoint"
)
print(doc, target = tempfile(fileext = ".docx"))
}
\seealso{
\code{\link[=table_xml]{table_xml()}}, \code{\link[=body_add_xml]{body_add_xml()}}, \code{\link[=table_header]{table_header()}},
\code{\link[=table_subheader]{table_subheader()}}

Other docx native-style table helpers:
\code{\link[=table_header]{table_header()}},
\code{\link[=table_subheader]{table_subheader()}},
\code{\link[=table_xml]{table_xml()}}
}
\concept{docx native-style table helpers}
