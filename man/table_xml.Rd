% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_table_xml.R
\name{table_xml}
\alias{table_xml}
\title{Word table markup (native styles + merges)}
\usage{
table_xml(
  value,
  style = "table_template",
  header = TRUE,
  stylenames = list(),
  header_style = NULL,
  col_widths = NULL,
  align = "center",
  merge_consecutive = NULL,
  tcf = table_conditional_formatting(),
  repeat_header = TRUE
)
}
\arguments{
\item{value}{a data.frame, or a list whose elements are data.frames and
\code{\link[=table_subheader]{table_subheader()}} objects. All data.frames must share identical column
names in the same order. A list lets you intersperse full-width sub-header
divider rows between groups of rows.}

\item{style}{table style name defined in the document (e.g. \code{"table_template"}
or a corporate template style).}

\item{header}{\code{TRUE} (default: one header row from the column names), \code{FALSE}
(no header), or a \code{\link[=table_header]{table_header()}} object (grouped / merged header).}

\item{stylenames}{paragraph styles for body cells, per column. A named list
or named character vector (column name -> style name); columns not listed
default to \code{"Normal"}. A \code{\link[=table_stylenames]{table_stylenames()}} object is also accepted.}

\item{header_style}{optional single paragraph style applied to every header
cell. If \code{NULL} (default), header cells inherit the per-column \code{stylenames},
matching \code{\link[=body_add_table]{body_add_table()}}.}

\item{col_widths}{optional column widths in inches, length equal to the
number of columns. When provided, a fixed \verb{<w:tblGrid>} is emitted.}

\item{align}{table alignment: one of \code{"left"}, \code{"center"} (default) or
\code{"right"}.}

\item{merge_consecutive}{optional character vector of column names whose
consecutive identical values are vertically merged (\verb{<w:vMerge>}) in the
body. A sub-header row resets the run, so merging restarts after each
divider.}

\item{tcf}{conditional formatting (table look), see
\code{\link[=table_conditional_formatting]{table_conditional_formatting()}}.}

\item{repeat_header}{logical, default \code{TRUE}; mark header rows as repeating
on every page (\verb{<w:tblHeader/>}).}
}
\value{
A length-1 character string of WordprocessingML for the table. Inject
it with \code{\link[=body_add_xml]{body_add_xml()}} or use \code{\link[=body_add_table_xml]{body_add_table_xml()}}.
}
\description{
Build the WordprocessingML (\verb{<w:tbl>}) for a table that inherits
the document template's named table/paragraph styles \strong{and} can express
grouped headers, vertical merges and full-width sub-header rows - the
combination \code{\link[=block_table]{block_table()}} / \code{\link[=body_add_table]{body_add_table()}} deliberately cannot express.

This is a fork-local helper built on \code{\link[=body_add_xml]{body_add_xml()}} (see upstream issue
#731). The returned string uses the same \code{w:tstlname} / \code{w:pstlname}
placeholder mechanism as officer's built-in table blocks, so the styles are
resolved to real style ids when the document is saved.
}
\examples{
library(officer)

df <- data.frame(g = c("A", "A", "B"), x = c(1, 2, 3), y = c(4, 5, 6))

# grouped (merged) header + vertically merged body column
wml <- table_xml(
  df,
  style = "table_template",
  header = table_header(list(
    Group  = c("g" = "g"),
    Values = c("x" = "x", "y" = "y")
  )),
  merge_consecutive = "g"
)

# sub-header dividers between groups of rows
wml2 <- table_xml(
  list(
    table_subheader("Category 1"),
    df,
    table_subheader("Category 2"),
    df
  ),
  style = "table_template",
  merge_consecutive = "g"
)
}
\seealso{
\code{\link[=body_add_table_xml]{body_add_table_xml()}}, \code{\link[=body_add_xml]{body_add_xml()}}, \code{\link[=table_header]{table_header()}},
\code{\link[=table_subheader]{table_subheader()}}

Other docx native-style table helpers:
\code{\link[=body_add_table_xml]{body_add_table_xml()}},
\code{\link[=table_header]{table_header()}},
\code{\link[=table_subheader]{table_subheader()}}
}
\concept{docx native-style table helpers}
